[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
prefect = ">=2.10.0"
prefect-aws = ">=0.3.0"
boto3 = ">=1.26.0"
pandas = ">=1.5.0"
xgboost = ">=1.7.0"
evidently = ">=0.4.0"
scikit-learn = ">=1.2.0"
requests = ">=2.28.0"
geopy = ">=2.3.0"
awscli = "*"

[dev-packages]

[requires]
python_version = "3.12"
